# SeenYT Internationalization Notes

Phase 5 added shared app copy for Vietnamese, English, and Japanese.

## Locale Config

Configured locales:

- `vi`
- `en`
- `ja`

Config file:

- `next-i18next.config.js`

## Shared Creator App Copy

Source file:

- `lib/creator-i18n.ts`

It currently covers:

- Dashboard home copy.
- Sidebar navigation copy.
- Mobile navigation copy.
- Workflow names, subtitles, and step labels.
- Tool names and descriptions.

## Files Using Shared Copy

- `components/dashboard/DashboardHome.tsx`
- `components/dashboard/Sidebar.tsx`
- `components/dashboard/MobileNav.tsx`
- `pages/index.tsx` uses the same locale helper and already has tri-locale homepage copy.

## Current Locale Route Smoke Test

After restarting the dev server, these routes returned HTTP 200:

- `/`
- `/en`
- `/ja`

## Remaining Work

- Move homepage copy out of `pages/index.tsx` into a shared copy module or locale JSON.
- Replace old corrupted strings in legacy locale JSON files.
- Localize deeper tool UIs after each tool is confirmed as Keep or Merge.
- Avoid literal translation. Keep the same product meaning in each market.

